{
  "name": "bridge",
  "version": "1.0.0",
  "description": "a chat bridge between platforms",
  "main": "build/index.ts",
  "scripts": {
    "start": "npx tsc && node build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cutzel",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "mineflayer": "^4.20.0",
    "telegraf": "^4.16.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.4.4",
    "@types/node": "^20.12.5"
  }
}
